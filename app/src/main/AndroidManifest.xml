<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <queries>
        <package android:name="com.google.android.gms" />
    </queries>

    <application
        android:name=".UGBApplication"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@drawable/logo_ugb"
        android:label="@string/app_name"
        android:roundIcon="@drawable/logo_ugb"
        android:supportsRtl="true"
        android:theme="@style/Theme.UGBMenu">

        <activity
            android:name=".SplashActivity"
            android:exported="true"
            android:theme="@style/Theme.UGBMenu">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".MainActivity"
            android:exported="false"
            android:label="@string/app_name"
            android:theme="@style/Theme.UGBMenu" />

        <activity
            android:name=".MealDetailActivity"
            android:exported="false"
            android:theme="@style/Theme.UGBMenu" />

        <activity
            android:name=".admin.AdminLoginActivity"
            android:exported="true"
            android:label="Login Admin" />

        <activity
            android:name=".admin.AdminDashboardActivity"
            android:exported="false"
            android:label="Dashboard Admin" />

        <activity
            android:name=".admin.ManageMenusActivity"
            android:exported="false"
            android:label="Gérer les Menus" />

        <activity
            android:name=".admin.EditMealActivity"
            android:exported="false"
            android:label="Modifier Plat" />

        <activity
            android:name=".admin.ReviewModerationActivity"
            android:exported="false"
            android:label="Modération Avis" />

        <receiver
            android:name=".utils.MenuWidgetProvider"
            android:exported="true">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/widget_info" />
        </receiver>

    </application>

</manifest>
